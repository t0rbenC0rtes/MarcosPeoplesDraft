// // Wait for React to render the SVG
// setTimeout(() => {
//   const pathLength = document.querySelectorAll("#logo3 path");
//   console.log(`Found ${pathLength.length} paths`);
//   for (let i = 0; i < pathLength.length; i++) {
//     console.log(`Path ${i} length: ${pathLength[i].getTotalLength()}`);
//   }
// }, 100); // Small delay to let React render
